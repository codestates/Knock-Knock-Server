// const express = require("express");
// const profileRouter = express.Router();
// const { profileController } = require("../controller");
// //유저의 프로필을 가져온다.
// profileRouter.get("/:id", profileController.getInfo.get);

// //유저의 최초 프로필을 등록한다.
// profileRouter.post("/:id", profileController.postInfo.post);

// //유저의 프로필을 수정한다.
// profileRouter.put("/:id", profileController.putInfo.put);

// export default profileRouter;
